import React from 'react'

const Home = () => (
	<>
		<h4>yyy</h4>
	</>
)
// const keyword = match.params.keyword
/*
	useEffect(() => {
		console.log('key11:', keyword)
		fetchArticleKeyword(keyword)
	}, [])
	return (
		<>
			<h4>yyy</h4>
		</>
	)
}

export default connect(null, { fetchArticleKeyword })(Home)
*/
export default Home
